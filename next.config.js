// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    // Removed output: 'export' to allow dynamic routes
    // If you need static export, use generateStaticParams in dynamic routes
};

module.exports = nextConfig;
